// Mirrors the tsconfig create-colyseus-app generates: strict on, but
// strictNullChecks off (and skipLibCheck on, as such consumers set it).
{
  "extends": "./tsconfig.types-decorators.json",
  "compilerOptions": {
    "strictNullChecks": false,
    "skipLibCheck": true
  },
  "files": [
    "test/types/SchemaTypeOptionality.nostrict.ts",
    "test/types/ComplexDeclarations.nostrict.ts"
  ]
}
